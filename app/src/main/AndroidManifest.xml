<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pwmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PWManager">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SettingNoticeActivity" />
        <activity android:name=".SettingDoubleLockActivity" />
        <activity android:name=".SettingThemeActivity" />
        <activity android:name=".SettingMasterActivity" />
        <activity android:name=".PopUpActivity" />
        <activity android:name=".PassInfo"></activity>
        <activity android:name=".MainActivity">
        </activity>
    </application>

</manifest>